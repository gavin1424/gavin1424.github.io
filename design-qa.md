# Design QA｜StudioSite v2 完整視覺改版

## 比對基準

- Source visual truth：`C:\Users\wwwas\Documents\Codex\2026-07-22\ai-c-users-wwwas-desktop-ai\.codex-remote-attachments\019f8a43-21a5-7bb2-8eb8-ebc6cbc4b091\fdc410db-aede-4ee1-a6bd-a4d90142ad4a\1-Photo-1.jpg`
- Implementation screenshot：`C:\Users\wwwas\Desktop\AI網站接案工作室\12_品牌與行銷素材\網站服務銷售頁_v2_改版後_1440.png`
- Combined comparison：`C:\Users\wwwas\Desktop\AI網站接案工作室\12_品牌與行銷素材\網站服務銷售頁_v2_參考圖並排比對.png`
- 主要比較 viewport：1440 × 900 CSS px，device scale factor 1
- Source pixels：1280 × 800
- Implementation pixels：1404 × 891（瀏覽器截圖扣除捲軸與瀏覽器可用區）
- 正規化：兩張圖均縮放為 1280 × 800 後水平並排；不以瀏覽器網址列差異作為版面問題
- State：首頁頂端、選單關閉、Hero 動效完成

## 全畫面比對

並排圖已同時呈現參考圖與實作首屏，可直接檢查：

- 頂部暖白導覽列、左品牌、中導覽、右金色 CTA
- 左文案、中裝置展示、右價格卡三區比例
- 黑色襯線大標題與金色小標
- 暖白／米色留白、自然光場景與極淡邊框
- Hero 下緣五項服務優勢

## 重要區域比對

不另做小尺寸局部拼圖：1280 × 800 正規化並排圖已能清楚閱讀標題、價格、按鈕與裝置螢幕。另分別開啟原始參考圖與 1440 實作截圖，確認字體、價格卡細節、圖片清晰度與五項優勢。

## 必要品質面向

### 字體與層級

- 中文大標使用 `Noto Serif TC` 與可靠繁體中文襯線後備。
- 內文及按鈕使用 `Noto Sans TC` 與系統黑體後備。
- Hero 標題、段落、補充、CTA 與三項重點的字級和行距皆有明確層級。
- 390px 與 360px 標題維持兩行，不截斷、不縮成桌面版。

### 間距與構圖

- 1440px 為三區 Hero；左文案約 40%、中裝置約 36%、右價格卡約 24%。
- 價格卡向下錯位，首屏下緣可看到五項優勢。
- 1024px 使用雙欄 Hero，價格卡移到下方；768px 以下改為單欄閱讀順序。
- 所有測試尺寸無橫向溢位。

### 色彩與視覺 Token

- 主背景 `#FCFBF8`、次背景 `#F3EEE5`、卡片 `#FFFDFC`。
- 主文字 `#201C18`、次文字 `#6B635A`、說明文字 `#887F75`。
- 金色只用於 CTA、價格、小標、圖示與少量線條。
- 未使用藍紫 SaaS 漸層、玻璃擬態、發光或大面積金色文字。

### 圖片品質與素材

- Hero 場景為本次新生成的無文字暖白石材場景，沒有 AI 螢幕文字。
- 筆電與手機螢幕使用暖線拼布示範站的真實瀏覽器截圖。
- 案例桌面與手機縮圖使用真實 viewport 截圖；概念版型清楚標示「概念展示」。
- 圖片採 WebP；非首屏案例圖片使用 lazy loading；所有有內容意義的圖片有 alt。

### 文案與真實性

- 主標已改為「一次建站，完整交付」。
- 保留正式售價 NT$12,800、前三位案例價 NT$9,800、50% 訂金／尾款、7 個工作天與兩次修改。
- 沒有假聯絡資料、假評價、假合作數字、效果保證、永久免費或終身維護承諾。

## 比較歷史

### 第一輪

- [P1] 中間圖片仍像單張情境合成，真實網站畫面不夠突出。
  - 修正：改為無文字場景背景，另以 HTML／CSS 筆電與手機 Mockup 嵌入真實暖線站截圖。
- [P1] 主標仍以「免主機月租」為主要注意點。
  - 修正：改為「一次建站／完整交付」，並同步 SEO、Open Graph 與 JSON-LD。
- [P2] 桌面首屏下方優勢未自然露出。
  - 修正：縮減 Hero 下方留白及價格卡高度，讓五項優勢在 1440 × 900 首屏可見。
- [P2] 手機固定 CTA 在頁面初始狀態遮住 Hero Mockup。
  - 修正：Hero 原始 CTA 可見時隱藏固定 CTA；離開 Hero 後才顯示，頁尾進入視窗時再次隱藏。
- [P2] JavaScript `window.open()` 在測試瀏覽器被攔截。
  - 修正：所有正式詢問動作改為真正的 `<a>` 連結，網址由 `site-config.js` 集中設定，實測可導向 Google 表單。
- [P1] GitHub Pages 更新後，瀏覽器仍從快取載入舊版設定檔，造成 Title 與 Open Graph 被舊資料覆寫。
  - 修正：為 `styles.css`、`site-config.js` 與 `script.js` 加入 v2 版本查詢字串，正式網址重新載入後即使用新版 SEO 與樣式。

### 第二輪證據

- 最終 1440px：`網站服務銷售頁_v2_改版後_1440.png`
- 最終 1024px：`網站服務銷售頁_v2_改版後_1024.png`
- 最終 768px：`網站服務銷售頁_v2_改版後_768.png`
- 最終 390px：`網站服務銷售頁_v2_改版後_390.png`
- 最終 360px：`網站服務銷售頁_v2_改版後_360.png`
- Browser console error：0
- 破圖：0
- 導覽、FAQ、正式 Google 表單入口：通過

## Findings

目前沒有待修正的 P0、P1 或 P2。參考圖中的客戶感言沒有照搬，因目前沒有取得真實授權評價；依需求改為真實方案資訊、案例與 FAQ。

## Follow-up Polish

- [P3] 未來取得真實授權案例後，可用正式客戶案例替換概念展示。
- [P3] Google 方案詢問表仍保留既有表單名稱，避免本次視覺改版影響已驗證的表單設定與回覆流程。

final result: passed
