# Design QA｜參考圖與實作比對

## 比對範圍

- 視覺來源：使用者提供的 StudioSite 參考圖片。
- 實作畫面：1440 × 900 的網站首屏。
- 同畫面比對：`12_品牌與行銷素材／網站服務銷售頁_參考圖首屏比對.jpg`
- 另外檢查：1024 × 768、768 × 1024、390 × 844。

## 參考圖還原結果

- 首屏採左側文案、中間筆電場景、右側價格卡的三區構圖。
- 中文大標題使用高對比明體，內文與操作元件使用清楚黑體。
- 使用暖白、米白、沙色與低彩度金色；移除舊版深色大卡、藍紫漸層與裝飾性圖形。
- 中間場景使用獨立新生成的自然光筆電與陶瓷花器照片，螢幕嵌入暖線示範站實際畫面。
- 價格卡使用細邊框、極淡陰影、金色案例價與完整範圍說明。
- Hero 下方以五張細邊框信任重點卡延續參考圖密度。
- 流程與 FAQ 在桌面並排；流程採淡金連接線，FAQ 使用簡潔手風琴。
- 底部 CTA 使用暖米色橫幅與單一 Google 方案詢問入口。

## 已發現並修正

### P1｜1024px 首屏資訊密度偏低

- 初版在 1024px 過早把 Hero 文案移到整列，右側留白過多。
- 已改為 1024px 保留三欄比例，並縮小標題與價格卡內距。

### P1｜詢問按鈕在測試瀏覽器被阻擋

- 初版使用 `window.open()` 開新分頁，在測試環境未建立新分頁。
- 已改為同頁前往設定檔中的 Google 表單，實際開啟成功。

### P2｜390px 標題換行被舊樣式覆蓋

- 舊版基礎 CSS 在小螢幕隱藏 `<br>`，使「工作室網站」拆成不理想的兩行。
- 已在新版覆寫，小螢幕維持「免主機月租／工作室網站」兩行。

### P2｜手機選單文字換行

- 初版選單按鈕寬度沿用舊規則，390px 顯示成垂直文字。
- 已加入合理最小寬度與不換行設定。

### P2｜金色按鈕對比

- 淺金底配白字的對比不足。
- 主要按鈕與小型金色標籤改用較深金色 `#8F6B32`，白字對比約 4.86:1。

## 無障礙與狀態

- 所有內容圖片均有 `alt`；裝飾圖示使用空 `alt` 與 `aria-hidden`。
- 導覽、CTA 與 FAQ 皆使用原生可聚焦元素。
- FAQ 的 Enter 鍵開合已在瀏覽器驗證。
- 手機選單使用原生 `button`、`aria-expanded` 與 Escape 關閉邏輯。
- 所有動效均受 `prefers-reduced-motion` 控制。

## 未發現的阻塞問題

- 沒有橫向溢位。
- 沒有破圖。
- 沒有假客戶評價、假聯絡方式或保證成效文案。
- 沒有 P0、P1 或 P2 未修正項目。

final result: passed
